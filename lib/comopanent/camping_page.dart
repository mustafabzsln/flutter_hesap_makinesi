import 'package:flutter/material.dart';

class campingPage extends StatelessWidget {
  const campingPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Camping Page"),
      ),
      body: Container(),
    );
  }
}
